package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Core Colors (Matching user image mockup)
val MarketPrimary = Color(0xFF0284C7)      // Vibrant Cyan/Sky Blue
val MarketPrimaryDark = Color(0xFF0369A1)
val MarketPrimaryLight = Color(0xFFE0F2FE)
val MarketBuySellPink = Color(0xFFE11D48)   // "Buy Sell" pink badge from image
val MarketBuySellPurple = Color(0xFF9333EA)
val FeaturedPink = Color(0xFFEC4899)

// Light Theme Canvas & Surfaces (Adjusted for comfort, zero glare, high contrast)
val LightBg = Color(0xFFF1F5F9)             // Soft slate background (eliminates blinding brightness)
val LightSurface = Color(0xFFFFFFFF)        // Pure crisp white card surface
val LightSurfaceVariant = Color(0xFFE2E8F0) // Clean container fill with strong contrast
val LightBorder = Color(0xFFCBD5E1)         // Defined card & divider border
val LightBorderStrong = Color(0xFF94A3B8)   // High-contrast border
val LightTextPrimary = Color(0xFF0F172A)    // Deep slate 900 - ultra-sharp contrast
val LightTextSecondary = Color(0xFF334155)  // Slate 700 - clearly legible text
val LightTextMuted = Color(0xFF64748B)      // Slate 500 - distinct, non-faded text

// Authentic Social Brand Colors
val TelegramCyan = Color(0xFF0284C7)
val TelegramBlueDark = Color(0xFF0369A1)
val WhatsAppGreen = Color(0xFF15803D)       // Darker, high-contrast WhatsApp Green
val WhatsAppDark = Color(0xFF166534)
val TikTokBlack = Color(0xFF000000)
val TikTokPink = Color(0xFFE11D48)
val InstagramGradient1 = Color(0xFF833AB4)
val InstagramGradient2 = Color(0xFFC13584)
val InstagramGradient3 = Color(0xFFF77737)
val FacebookBlue = Color(0xFF1D4ED8)        // High-contrast Facebook Blue
val YouTubeRed = Color(0xFFDC2626)          // Vivid red
val XTwitterBlack = Color(0xFF0F172A)
val DiscordIndigo = Color(0xFF4F46E5)

// Badges & Actions
val TonGold = Color(0xFFD97706)             // High-contrast amber/gold (clearly visible on white)
val TonActiveGreen = Color(0xFF047857)      // High-contrast green
val VerifiedBadgeBlue = Color(0xFF0284C7)
val PremiumPurple = Color(0xFF7C3AED)       // Deep purple
val PriceGreen = Color(0xFF047857)          // Emerald 700 - ultra-legible price color
val DangerRed = Color(0xFFDC2626)
val SoldGray = Color(0xFF64748B)

// Deprecated dark aliases kept for backwards safety
val DarkBg = LightBg
val DarkSurface = LightSurface
val DarkSurfaceVariant = LightSurfaceVariant
val DarkBorder = LightBorder
val DarkTextPrimary = LightTextPrimary
val DarkTextSecondary = LightTextSecondary
val DarkTextMuted = LightTextMuted

